# node / cdk
node_modules/
cdk.out/
*.tsbuildinfo
# tsc emit; cdk runs ts-node straight off the .ts sources
infra/**/*.js
infra/**/*.d.ts

# python
__pycache__/
*.pyc
.venv/
venv/

# secrets and local env (never commit)
.env
*.env
secrets.json
token.json
client_secret*.json

# cdk deploy outputs (contain account-specific urls, regenerate per deploy)
cdk-outputs.json

# os
.DS_Store
